@extends('layouts.dashboard') @section('title',$rubric->name) @section('breadcrumb','Detail rubrik')
@section('content')<div class="grading-page"><div class="grading-hero"><div><p>RUBRIK REUSABLE</p><h1>{{ $rubric->name }}</h1><span>{{ $rubric->criteria->count() }} kriteria · Total {{ $rubric->criteria->sum('weight') }}%</span></div><a class="btn btn-light" href="{{ route('teacher.rubrics.edit',$rubric) }}">Edit rubrik</a></div><div class="rubric-table">@foreach($rubric->criteria as $criterion)<article><header><h2>{{ $criterion->name }}</h2><strong>{{ $criterion->weight }}%</strong></header><div>@foreach($criterion->levels as $level)<section><span>{{ $level->level }}</span><strong>{{ $level->label }}</strong><p>{{ $level->description }}</p></section>@endforeach</div></article>@endforeach</div></div>@endsection
