@extends('layouts.dashboard') @section('title','Rubrik Penilaian') @section('breadcrumb','Rubrik')
@section('content')<div class="grading-page"><x-ui.page-header eyebrow="Penilaian" title="Rubrik reusable" description="Susun kriteria berbobot dan capaian level 1–4 untuk digunakan pada banyak tugas."><x-slot:actions><x-ui.button :href="route('teacher.rubrics.create')" icon="bi-plus-lg">Buat rubrik</x-ui.button></x-slot:actions></x-ui.page-header>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="rubric-grid">@forelse($rubrics as $rubric)<a href="{{ route('teacher.rubrics.show',$rubric) }}"><span>{{ $rubric->criteria_count }} kriteria · {{ $rubric->assignments_count }} tugas</span><h2>{{ $rubric->name }}</h2><p>{{ $rubric->description }}</p><strong>{{ $rubric->is_active?'Aktif':'Nonaktif' }}</strong></a>@empty<x-ui.empty-state title="Belum ada rubrik" description="Buat rubrik pertama dengan total bobot 100%." icon="bi-ui-checks-grid"/>@endforelse</div>{{ $rubrics->links() }}</div>@endsection
