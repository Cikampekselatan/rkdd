@extends('layouts.dashboard') @section('title','Tugas - SKUAD Learning Hub') @section('breadcrumb','Tugas')
@section('content')<div class="assignment-page"><x-ui.page-header eyebrow="Pembelajaran" title="Tugas dan submission" description="Atur tugas, tenggat, file, keterlambatan, serta revisi siswa."><x-slot:actions><x-ui.button :href="route('teacher.assignments.create')" icon="bi-plus-lg">Buat tugas</x-ui.button></x-slot:actions></x-ui.page-header>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="assignment-grid">@forelse($assignments as $assignment)<a class="assignment-card" href="{{ route('teacher.assignments.show',$assignment) }}"><div class="assignment-card-top"><span>{{ $assignment->type->label() }}</span><strong class="{{ $assignment->is_published?'published':'draft' }}">{{ $assignment->is_published?'Terbit':'Draf' }}</strong></div><h2>{{ $assignment->title }}</h2><p>Pertemuan {{ $assignment->learningSession->session_number }} · {{ $assignment->schoolClass->name }}</p><div><span><i class="bi bi-calendar-event"></i>{{ $assignment->due_at->translatedFormat('d M Y H:i') }}</span><span><i class="bi bi-inbox"></i>{{ $assignment->submissions_count }} submission</span></div></a>@empty<x-ui.empty-state title="Belum ada tugas" description="Buat tugas pertama dan hubungkan ke pertemuan." icon="bi-clipboard-plus" />@endforelse</div>{{ $assignments->links() }}</div>@endsection
