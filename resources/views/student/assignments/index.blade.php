@extends('layouts.dashboard') @section('title','Tugas Saya') @section('breadcrumb','Tugas saya')
@section('content')<div class="assignment-page"><x-ui.page-header eyebrow="Ruang kerja" title="Tugas saya" description="Simpan draf, kirim karya, dan pantau permintaan revisi."/><div class="assignment-grid">@forelse($assignments as $assignment)@php($submission=$assignment->submissions->first())<a class="assignment-card" href="{{ route('student.assignments.show',$assignment) }}"><div class="assignment-card-top"><span>{{ $assignment->type->label() }}</span><strong class="{{ $submission?'published':'draft' }}">{{ $submission?->status->label()??'Belum mulai' }}</strong></div><h2>{{ $assignment->title }}</h2><p>Pertemuan {{ $assignment->learningSession->session_number }}</p><div><span><i class="bi bi-clock"></i>{{ $assignment->due_at->diffForHumans() }}</span><span>{{ $submission?'Versi '.$submission->current_version_number:'Buka tugas' }}</span></div></a>@empty<x-ui.empty-state title="Belum ada tugas aktif" description="Tugas yang dipublikasikan pembina akan muncul di sini." icon="bi-clipboard-check"/>@endforelse</div>{{ $assignments->links() }}</div>@endsection
